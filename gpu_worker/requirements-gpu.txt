# GPU Worker Dependencies with CUDA Support for L4 GPUs
# Core web framework
flask==3.1.2
gunicorn==22.0.0

# Google Cloud dependencies  
google-cloud-firestore==2.21.0
google-cloud-storage==3.3.0
google-cloud-logging==3.11.4

# PyTorch with CUDA 12.1 support for L4 GPUs (installed separately in Dockerfile)
# torch>=2.0.0
# torchaudio>=2.0.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Boltz-2 dependencies (boltz itself installed from GitHub in Dockerfile)
# The following are dependencies that Boltz-2 requires
lightning>=2.0.0
hydra-core>=1.3.0
einops>=0.7.0
dm-tree>=0.1.8

# Essential utilities
python-dotenv==1.1.1
pyyaml>=6.0
requests>=2.31.0

# Molecular structure handling
rdkit>=2023.9.1
biopython>=1.81

# Performance monitoring
psutil>=5.9.0