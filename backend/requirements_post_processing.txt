# Post-Processing Dependencies for Boltz-2 Scientific Analysis
# High-performance contact analysis and clustering

# Core scientific computing
MDAnalysis>=2.6.1
numpy>=1.24.0
scipy>=1.10.0

# Machine learning for clustering
scikit-learn>=1.3.0

# Chemical informatics
rdkit>=2023.3.1

# Performance optimizations (optional but recommended)
numba>=0.57.0  # JIT compilation for contact calculations
cython>=0.29.0  # Compiled extensions

# Installation command:
# pip install -r requirements_post_processing.txt

# For conda users:
# conda install -c conda-forge mdanalysis scikit-learn rdkit numba

# Note: These dependencies are optional
# If not installed, the system will gracefully fallback to mock data
# Install only when ready to use real post-processing features